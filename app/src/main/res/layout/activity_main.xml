<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="trinadhkoya.gihub.io.gettingstartedfcm_chat.MainActivity">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textEmailAddress"
        android:ems="10"
        android:text="trinadhkoya9@gmail.com"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="159dp"
        android:id="@+id/email_edit_text"
        android:fontFamily="sans-serif"
        android:hint="Enter your E-mal" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:ems="10"
        android:text="iP@ssword71"
        android:layout_below="@+id/email_edit_text"
        android:layout_alignRight="@+id/email_edit_text"
        android:layout_alignEnd="@+id/email_edit_text"
        android:layout_marginTop="32dp"
        android:id="@+id/password_edit_text"
        android:hint="Password" />

    <Button
        android:text="Login"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/password_edit_text"
        android:layout_alignLeft="@+id/password_edit_text"
        android:layout_alignStart="@+id/password_edit_text"
        android:layout_marginTop="46dp"
        android:textColor="#ffffff"
        android:layout_alignRight="@+id/password_edit_text"
        android:layout_alignEnd="@+id/password_edit_text"
        android:background="@color/colorPrimary"
        android:id="@+id/login_button" />

    <Button
        android:text="Register"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/login_button"
        android:textColor="#ffffff"
        android:layout_alignLeft="@+id/login_button"
        android:layout_alignStart="@+id/login_button"
        android:layout_marginTop="10dp"
        android:id="@+id/register_button"
        android:background="@color/colorPrimaryDark"
        android:layout_alignRight="@+id/login_button"
        android:layout_alignEnd="@+id/login_button" />
</RelativeLayout>
